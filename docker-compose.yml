services:
  react_bwc_ambassador:
    image: react_bwc_ambassador
    build:
      context: .
      dockerfile: ./Dockerfile
    # environment:
    #   NODE_ENV: development
    ports:
      - "3000:8080"